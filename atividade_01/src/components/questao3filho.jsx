import React from 'react';

export default props => {
    return (
        <div>
            Oi eu sou o {props.nome}
             <strong>faco o curso de {props.curso}</strong>
             moro na cidade de {props.cidade}
        </div>
    )
    
}